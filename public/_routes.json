{
  "version": 1,
  "description": "Run Pages Functions only for legacy WordPress ghost paths. Every other route stays a pure static file (no Function cold-start, no per-request CPU). See functions/_middleware.ts.",
  "include": [
    "/tag/*",
    "/category/*",
    "/author/*",
    "/page/*",
    "/feed",
    "/feed/",
    "/comments/*",
    "/trackback/*",
    "/wp-admin",
    "/wp-admin/*",
    "/wp-login.php",
    "/wp-content/*",
    "/wp-includes/*",
    "/wp-json/*",
    "/xmlrpc.php"
  ],
  "exclude": [
    "/blog/category/*"
  ]
}
